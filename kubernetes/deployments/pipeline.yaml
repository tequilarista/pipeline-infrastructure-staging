apiVersion: apps/v1beta1
kind: Deployment
metadata:
  labels:
    app: pipeline
  name: pipeline
  namespace: default
spec:
  selector:
    matchLabels:
      app: pipeline
  template:
    metadata:
      labels:
        app: pipeline
    spec:
      containers:
      - image: gcr.io/concise-base-204600/pipeline:774dd306d28de83ee596ff71323c0043fd3b4a73
        name: pipeline
        ports:
        - containerPort: 80
          name: http
          protocol: TCP
