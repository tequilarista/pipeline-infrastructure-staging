apiVersion: apps/v1beta1
kind: Deployment
metadata:
  labels:
    app: pipeline
  name: pipeline
  namespace: default
spec:
  selector:
    matchLabels:
      app: pipeline
  template:
    metadata:
      labels:
        app: pipeline
    spec:
      containers:
      - image: gcr.io/concise-base-204600/pipeline:2d16fb359e5c4d2b01849bfb328d2b40cd0be973
        name: pipeline
        ports:
        - containerPort: 80
          name: http
          protocol: TCP
